pipeline {
    agent any

    environment {
        // Définir les variables d'environnement nécessaires
        NODEJS_VERSION = '18.14'
    }

    stages {
        stage('Check Version') {
            steps {
                sh "node --version"
                sh "npm --version"
            }
        }



  
}
